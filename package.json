{
  "name": "whoisit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Qui a dit ça ? — jeu de devinettes sur vos conversations, 100 % client-side.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "audit:privacy": "node scripts/audit-privacy.mjs",
    "verify": "npm run check && npm run test && npm run build && npm run audit:privacy",
    "test": "vitest run",
    "test:watch": "vitest",
    "analyze": "vite-node scripts/analyze-pool.ts"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "svelte": "^5.56.9"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tailwindcss/vite": "^4.3.3",
    "@testing-library/svelte": "^5.4.2",
    "@types/node": "^26.2.0",
    "jsdom": "^30.0.1",
    "svelte-check": "^4.7.6",
    "tailwindcss": "^4.3.3",
    "typescript": "~5.9",
    "vite": "^8.2.1",
    "vite-node": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
